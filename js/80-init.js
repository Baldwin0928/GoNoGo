// Reserved for a future module boot path.
// The current no-build runtime loads ordered classic scripts directly in index.html
// so the app still works when index.html is opened from the filesystem.
